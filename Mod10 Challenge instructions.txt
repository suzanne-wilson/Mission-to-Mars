# Obtain high-resolution images for each of Mars's hemispheres.
# NOTE
# You must click each hemisphere’s link to access the full-resolution image’s URL.

# Save both the image URL string (for the full-resolution image) and the hemisphere 
# title (with the name).
# Use a Python dictionary to store the data using the keys `img_url` and `title`.
# Append the dictionary with the image URL string and the hemisphere title to a 
# list. This list will contain one dictionary for each hemisphere.